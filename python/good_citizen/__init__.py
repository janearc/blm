"""good-citizen, Python side: the matched first-class counterpart to the Go
library. Python consumers (paling, magpie daemons) get the same citizen surface
Go consumers do, over the same generated contracts.
"""
